<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <LinearLayout
        android:id="@+id/ll_saldo_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:background="@drawable/bg_button_accept"
        android:orientation="horizontal"
        android:padding="@dimen/margen_pequeño"
        android:layout_marginTop="@dimen/margen_pequeño"
        android:layout_marginBottom="@dimen/margen_pequeño">

        <ImageView
            android:id="@+id/iv_agente"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:gravity="start"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_shopping_kart"
            android:contentDescription="@string/gen_desc"
            android:layout_marginEnd="4dp" />

        <TextView
            android:id="@+id/tv_agente"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="start|center_vertical"
            android:textSize="@dimen/tamaño_texto_standar"
            android:maxLines="1"
            android:textColor="@color/colorWhite" />

    </LinearLayout>
</layout>